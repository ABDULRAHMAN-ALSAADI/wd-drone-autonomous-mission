# Local Data

This folder is for generated camera images and experiment data. Those files
are intentionally not committed to GitHub because they can be large, change
frequently, and may contain poor or duplicate samples.

The OpenCV camera tools may create:

```text
data/camera_snapshots/
```

Keep original data backed up separately. Commit only small documentation or
manually reviewed metadata, never an accidental camera dump.
